import { Category } from './Category';

export class FrillCategory extends Category {
    constructor(weight) {
        super();

        // Set instance variables
        this.weight = weight;
    }
}